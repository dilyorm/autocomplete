{
  "name": "acomplete",
  "version": "0.1.0",
  "description": "Tab autocomplete for prompts typed into terminal coding agents (Claude Code, Codex)",
  "type": "module",
  "bin": {
    "acomplete": "bin/acomplete.js"
  },
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "node-pty": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
